services:
  db:
    image: mysql:8
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 12345
      MYSQL_DATABASE: dev
      MYSQL_USER: user
      MYSQL_PASSWORD: 12345
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: